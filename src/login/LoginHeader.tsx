import {
  Stack,
  Link,
  Heading,
  Text,
} from '@chakra-ui/react';

export default function LoginHeader() {
  return (
    <Stack align={'center'}>
      <Heading fontSize={'4xl'}>Sign in</Heading>
    </Stack>
  );
}
